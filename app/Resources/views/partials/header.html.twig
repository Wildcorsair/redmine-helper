<div class="container">
    <div class="row">
        <div id="app-header"class="col-md-12">
            <div id="app-logo" class="col-md-10">Redmine Helper</div>
            <div id="app-logout" class="col-md-2 pull-right">
                {% if app.user != null %}
                    <span id="app-username">{{ app.user.username }}</span> | <a href="{{ path('logout') }}">Logout</a>
                {% endif %}
            </div>
        </div>
    </div>
</div>
